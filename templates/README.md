# Production templates

The committed order is:

1. `template41.png` — 图2
2. `template51.png` — 图3
3. `template52.png` — 图4

The paths and ordering must match `REAL_TO_RENDER_TEMPLATE_PATHS`. Treat this
template set, the prompt, provider model, `SKING_DDJ_v54.pt`, and renderer
mappings as one immutable `REAL_TO_RENDER_PIPELINE_VERSION`.
